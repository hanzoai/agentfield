{{- if .Values.demoAgent.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentfield.demoAgent.fullname" . }}
  labels:
    {{- include "agentfield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.demoAgent.service.type }}
  selector:
    {{- include "agentfield.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: demo-agent
  ports:
    - name: http
      port: {{ .Values.demoAgent.service.port }}
      targetPort: http
{{- end }}

